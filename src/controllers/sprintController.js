require("dotenv").config();
const sprintService = require("../services/sprintService");

async function createSprint(params) {
    
}

async function pauseSprint(params) {
    
}

async function activeSprint(params) {
    
}

async function endSprint(params) {
    
}

module.exports = {
    createSprint,
    pauseSprint,
    activeSprint,
    endSprint
}